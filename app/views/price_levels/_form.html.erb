<%= form_for @price_level, :html => { :class => "form-horizontal price_level" } do |f| %>

  <% if @price_level.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@price_level.errors.count, "error") %> prohibited this price_level from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @price_level.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:name) %>
  </div>
  <div class="form-group">
    <%= f.label :brand_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= collection_select(:price_level, :brand_id, Brand.all, :id, :name, :prompt => "Select a Brand", :class => 'form-control') %>
    </div>
    <%=f.error_span(:brand_id) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                price_levels_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
