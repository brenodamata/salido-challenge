<%= form_for @order_type, :html => { :class => "form-horizontal order_type" } do |f| %>

  <% if @order_type.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@order_type.errors.count, "error") %> prohibited this order_type from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @order_type.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:name) %>
  </div>
  <div class="form-group">
    <%= f.label :brand_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= collection_select(:order_type, :brand_id, Brand.all, :id, :name, :prompt => "Select a Brand", :class => 'form-control') %>
    </div>
    <%=f.error_span(:brand_id) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                order_types_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
